version: '3'
services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        image: laravel-app
        container_name: laravel-app
        volumes:
            - .:/var/www
        ports:
            - "8000:9000"
        networks:
            - laravel-network

networks:
    laravel-network:
        driver: bridge
